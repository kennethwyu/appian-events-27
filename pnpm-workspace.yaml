allowBuilds:
  esbuild: set this to true or false

# Upstream builds with bun (flat node_modules) and imports several packages it
# only declares transitively. Hoisting keeps pnpm compatible.
nodeLinker: hoisted

onlyBuiltDependencies:
  - esbuild
